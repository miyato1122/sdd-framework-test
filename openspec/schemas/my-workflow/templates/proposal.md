## なぜ

<!-- この変更の動機を説明してください。どの問題を解決するのか、なぜ今やるのか。 -->

## 変更内容

<!-- 何が変わるかを記述してください。新規機能（capability）・修正・削除を具体的に。破壊的変更は **BREAKING** を付けてください。 -->

## 機能（Capabilities）

### 新規機能

<!-- 導入する機能（capability）。<name> は kebab-case 識別子（例: user-auth, data-export, api-rate-limiting）。各機能ごとに specs/<name>/spec.md が生成されます。 -->

- `<name>`: <この機能が扱う範囲の簡潔な説明>

### 変更機能

<!-- 既存機能のうち REQUIREMENTS が変わるもの（実装の変更のみは含めない）。
     仕様レベルの振る舞いが変わる場合のみここに記載してください。各機能ごとに delta spec ファイルが必要です。
     既存 spec 名は openspec/specs/ を参照してください。要件変更がなければ空のままで OK。 -->

- `<existing-name>`: <変わる要件の内容>

## 影響範囲

<!-- 影響を受けるコード・API・依存関係・システム -->
